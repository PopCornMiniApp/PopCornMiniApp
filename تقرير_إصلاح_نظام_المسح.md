# 🛠️ تقرير إصلاح نظام المسح - تسجيل جميع المسلسلات

**التاريخ:** 2026-05-09  
**الحالة:** ✅ تم التطبيق - قيد الاختبار

---

## 📋 ملخص المشكلة

### المشكلة المكتشفة
- **الواقع الفعلي:** 32 فيلم + 9 مسلسلات (25 موسم)
- **قاعدة البيانات:** 32 فيلم ✅ + 3 مسلسلات فقط ❌
- **المفقود:** 6 مسلسلات (From, Grimm, The Testaments, Lioness, Detective Hole, Code Black)

### السبب الجذري

في `app/scanner.py` السطر 55:
```python
elif parsed["type"] == "series":
    already = db.get_series(series_id=parsed["internal_id"])
    set_topic_series_map(topic_id, parsed["internal_id"])
    if already: return False  # ❌ المشكلة: يتوقف عند إيجاد المسلسل
```

**التفسير:**
- كل موسم له topic منفصل في Telegram
- عند مسح الموسم الأول → يسجل المسلسل ✅
- عند مسح المواسم التالية → يجد المسلسل موجود فيتوقف ❌
- النتيجة: فقط المسلسلات التي تم مسح موسمها الأول أولاً تُسجل

---

## 🔧 الحل المطبق

### 1. إضافة دالة استخراج رقم الموسم

```python
SEASON_NUM_RE = re.compile(r'#s(\d+)', re.IGNORECASE)

def extract_season_number(title: str) -> int:
    """استخراج رقم الموسم من عنوان الـ topic"""
    match = SEASON_NUM_RE.search(title)
    if match:
        return int(match.group(1))
    
    # Fallback: البحث عن "موسم X" أو "Season X"
    match = re.search(r'(?:موسم|season)\s*(\d+)', title, re.IGNORECASE)
    if match:
        return int(match.group(1))
    
    return 1  # افتراضي
```

### 2. تحديث منطق التسجيل

**قبل:**
```python
elif parsed["type"] == "series":
    already = db.get_series(series_id=parsed["internal_id"])
    set_topic_series_map(topic_id, parsed["internal_id"])
    if already: return False  # ❌ يتوقف
```

**بعد:**
```python
elif parsed["type"] == "series":
    season_num = extract_season_number(topic_title)
    already = db.get_series(series_id=parsed["internal_id"])
    set_topic_series_map(topic_id, parsed["internal_id"])
    
    if not already:
        # تسجيل المسلسل الجديد
        tmdb = await fetch_series(parsed["tmdb_id"])
        if not tmdb: return False
        db.upsert_series({"id": parsed["internal_id"], **tmdb})
        logger.info("[scanner] ✅ Registered NEW series: %s (Season %d)", 
                   tmdb['title'], season_num)
    else:
        logger.info("[scanner] ✓ Series exists: %s - processing Season %d", 
                   already['title'], season_num)
    
    # تسجيل الموسم
    season_exists = get_season(parsed["internal_id"], season_num)
    if not season_exists:
        upsert_season({
            "series_id": parsed["internal_id"],
            "season_number": season_num,
            "topic_id": topic_id,
            "name": f"Season {season_num}",
            "episode_count": 0,
            "air_date": None,
            "overview": None,
            "poster_path": None,
        })
    
    return True  # ✅ دائماً نكمل المسح
```

### 3. إضافة جدول Seasons

```sql
CREATE TABLE IF NOT EXISTS seasons (
    series_id       TEXT NOT NULL,
    season_number   INTEGER NOT NULL,
    topic_id        INTEGER,
    name            TEXT,
    episode_count   INTEGER DEFAULT 0,
    air_date        TEXT,
    overview        TEXT,
    poster_path     TEXT,
    created_at      TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (series_id, season_number),
    FOREIGN KEY (series_id) REFERENCES series(id)
);
```

### 4. إضافة دوال إدارة المواسم

في `app/database.py`:
- `upsert_season(data)` - إضافة أو تحديث موسم
- `get_season(series_id, season_number)` - الحصول على موسم محدد
- `get_series_seasons(series_id)` - الحصول على جميع مواسم مسلسل

---

## 📁 الملفات المعدلة

### 1. `PopCorn/app/scanner.py`
- ✅ إضافة `SEASON_NUM_RE` و `extract_season_number()`
- ✅ تحديث `_ensure_registered()` لمعالجة المواسم
- ✅ إزالة شرط `if already: return False`
- ✅ إضافة تسجيل المواسم في جدول seasons
- ✅ تحديث استدعاءات الدالة لتمرير `topic_title`

### 2. `PopCorn/app/database.py`
- ✅ إضافة جدول `seasons` في `_create_schema()`
- ✅ إضافة `upsert_season()`
- ✅ إضافة `get_season()`
- ✅ إضافة `get_series_seasons()`
- ✅ إضافة index `idx_seasons_series`

### 3. `PopCorn/verify_series_fix.py` (جديد)
- ✅ سكريبت للتحقق من نجاح الإصلاح
- ✅ يعرض عدد المسلسلات والمواسم والحلقات
- ✅ يقارن بالأرقام المتوقعة

---

## 🧪 الاختبار

### الأوامر
```bash
# تشغيل Full Scan
cd PopCorn
python3 trigger_fullscan.py

# التحقق من النتائج
python3 verify_series_fix.py

# فحص قاعدة البيانات
python3 check_db_counts.py
```

### النتائج المتوقعة

**قبل الإصلاح:**
- 🎬 32 فيلم ✅
- 📺 3 مسلسلات ❌
- 📁 ~15 موسم
- 🎞 ~200 حلقة

**بعد الإصلاح:**
- 🎬 32 فيلم ✅
- 📺 9 مسلسلات ✅
- 📁 25 موسم ✅
- 🎞 ~300+ حلقة ✅

---

## 🎯 المسلسلات المفقودة المتوقع تسجيلها

1. **From** (sid550) - 3 مواسم
2. **Grimm** (sid248) - 6 مواسم
3. **The Testaments** (sid126) - 1 موسم
4. **Lioness** (sid194) - 2 موسم
5. **Detective Hole** (sid?) - مواسم متعددة
6. **Code Black** (sid?) - مواسم متعددة

---

## 📊 حالة التنفيذ

- [x] تحليل المشكلة
- [x] تصميم الحل
- [x] تطبيق التعديلات على scanner.py
- [x] تطبيق التعديلات على database.py
- [x] إضافة جدول seasons
- [x] إضافة دوال إدارة المواسم
- [x] إنشاء سكريبت التحقق
- [ ] تشغيل Full Scan (قيد التنفيذ)
- [ ] التحقق من النتائج
- [ ] النشر على HuggingFace

---

## 🔍 ملاحظات فنية

### لماذا كان الحل ضرورياً؟

1. **البنية الحالية:** كل موسم = topic منفصل في Telegram
2. **المشكلة:** الكود القديم يتوقف عند إيجاد المسلسل
3. **الحل:** الاستمرار في المسح لتسجيل جميع المواسم والحلقات

### التحسينات الإضافية

1. **تتبع المواسم:** جدول منفصل لتتبع كل موسم
2. **Logging محسّن:** رسائل واضحة لتتبع التقدم
3. **Robustness:** معالجة الحالات الخاصة (مواسم بدون رقم، إلخ)

### الأمان

- ✅ لا يؤثر على البيانات الموجودة (32 فيلم + 3 مسلسلات)
- ✅ يضيف المسلسلات المفقودة فقط
- ✅ يحافظ على الحلقات المسجلة
- ✅ يستخدم `upsert` لتجنب التكرار

---

## 📝 الخطوات التالية

1. ✅ انتظار اكتمال Full Scan
2. ⏳ تشغيل `verify_series_fix.py`
3. ⏳ التحقق من ظهور المحتوى في Frontend
4. ⏳ رفع التحديثات إلى HuggingFace
5. ⏳ اختبار نهائي على Production

---

## 🎉 النتيجة المتوقعة

بعد تطبيق هذا الإصلاح:
- جميع المسلسلات التسعة ستكون مسجلة ✅
- جميع المواسم (25) ستكون مرتبطة بشكل صحيح ✅
- جميع الحلقات ستكون متاحة للمشاهدة ✅
- Frontend سيعرض المحتوى الكامل ✅

---

**تم بواسطة:** Bob (AI Assistant)  
**المراجعة:** قيد الانتظار  
**الموافقة:** قيد الانتظار