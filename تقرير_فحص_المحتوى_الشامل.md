# 🔍 تقرير فحص المحتوى الشامل - PopCorn

**التاريخ:** 2026-05-09  
**الهدف:** فحص المحتوى الفعلي في Telegram ومقارنته بقاعدة البيانات

---

## 📊 النتائج الرئيسية

### ✅ الأفلام - متطابقة تماماً
- **Telegram:** 32 فيلم
- **قاعدة البيانات:** 32 فيلم
- **الحالة:** ✅ **مزامنة كاملة - لا توجد مشاكل**

### ⚠️ المسلسلات - فجوة كبيرة
- **Telegram:** 9 مسلسلات فريدة (25 موسم إجمالاً)
- **قاعدة البيانات:** 3 مسلسلات فقط
- **الحالة:** ❌ **6 مسلسلات مفقودة**

---

## 📋 تفاصيل المحتوى في Telegram

### 🎬 الأفلام (32 فيلم) - ✅ الكل موجود
جميع الأفلام مسجلة بشكل صحيح في قاعدة البيانات:
- mid00001 إلى mid00033 (مع تخطي mid00028 و mid00034+)

### 📺 المسلسلات في Telegram (9 مسلسلات)

#### ✅ موجودة في قاعدة البيانات (3):
1. **Stranger Things** (sid00001)
   - 5 مواسم: S1, S2, S3, S4, S5
   - Topic IDs: 4653, 4665, 4684, 4693, 4703

2. **His & Hers** (sid00002) 
   - 1 موسم: S1
   - Topic ID: 4800

3. **My Brilliant Friend** (sid00003)
   - 4 مواسم: S1, S2, S3, S4
   - Topic IDs: 4807, 4816, 4826, 4835

#### ❌ مفقودة من قاعدة البيانات (6):

4. **From** (sid00004) - TMDB: 124364
   - 4 مواسم: S1, S2, S3, S4
   - Topic IDs: 4880, 4891, 4902, 4913

5. **Grimm** (sid00005) - TMDB: 39351
   - 6 مواسم: S1, S2, S3, S4, S5, S6
   - Topic IDs: 4917, 4940, 4963, 4986, 5009, 5031

6. **The Testaments** (sid00006) - TMDB: 287527
   - 1 موسم: S1
   - Topic ID: 5045

7. **Lioness** (sid00007) - TMDB: 113962
   - 2 موسم: S1, S2
   - Topic IDs: 5054, 5063

8. **Detective Hole** (sid00008) - TMDB: 249597
   - 1 موسم: S1
   - Topic ID: 5072

9. **Code Black** (sid00009) - TMDB: 63418
   - 1 موسم: S1
   - Topic ID: 5082

---

## 🔍 تحليل المشكلة

### لماذا المسلسلات مفقودة؟

بعد فحص الكود، وجدت المشاكل التالية:

#### 1. **مشكلة في نظام المسح** (`scanner.py`)
```python
# السطر 52-60: المشكلة هنا
elif parsed["type"] == "series":
    already = db.get_series(series_id=parsed["internal_id"])
    set_topic_series_map(topic_id, parsed["internal_id"])
    if already: return False  # ❌ يتوقف إذا كان موجود
```

**المشكلة:** 
- عند مسح موسم جديد لمسلسل موجود، يتوقف النظام
- لا يسجل المواسم الجديدة
- يفترض أن كل topic هو مسلسل جديد، لكن في الواقع كل topic هو موسم

#### 2. **عدم التمييز بين المسلسل والموسم**
```python
# نمط التعرف الحالي
SERIES_TOPIC_RE = re.compile(r'#([\w&]+)\s+#series\s+(?:#s\d+\s+)?#(sid\d+)\s+#(\d+)')
```

**المشكلة:**
- النمط يتعرف على `#s1`, `#s2` لكن لا يستخدمها
- لا يميز بين المسلسل الأساسي والمواسم المختلفة
- كل موسم له topic منفصل لكن نفس `sid`

#### 3. **جدول seasons غير موجود**
```
sqlite3.OperationalError: no such table: seasons
```

**المشكلة:**
- قاعدة البيانات لا تحتوي على جدول `seasons`
- النظام يخزن الحلقات مباشرة بدون تسجيل المواسم
- هذا يسبب مشاكل في التنظيم

---

## 🛠️ الحل المقترح

### المرحلة 1: إصلاح نظام المسح (عاجل)

#### أ. تحديث `scanner.py` - دالة `_ensure_registered`

```python
async def _ensure_registered(parsed: dict, topic_id: int) -> bool:
    if parsed["type"] == "movie":
        # ... كود الأفلام كما هو
        
    elif parsed["type"] == "series":
        series_id = parsed["internal_id"]
        
        # تسجيل المسلسل إذا لم يكن موجود
        already = db.get_series(series_id=series_id)
        if not already:
            tmdb = await fetch_series(parsed["tmdb_id"])
            if not tmdb:
                return False
            db.upsert_series({"id": series_id, **tmdb})
            logger.info("[scanner] Registered series: %s", tmdb['title'])
        
        # ربط الـ topic بالمسلسل (مهم لكل موسم)
        set_topic_series_map(topic_id, series_id)
        
        # استخراج رقم الموسم من عنوان الـ topic
        season_match = re.search(r'#s(\d+)', parsed.get("topic_title", ""), re.IGNORECASE)
        if season_match:
            season_num = int(season_match.group(1))
            
            # تسجيل الموسم في جدول seasons (سنضيفه)
            # أو على الأقل تسجيل أن هذا الموسم موجود
            logger.info(f"[scanner] Season {season_num} for series {series_id}")
        
        return not already  # True إذا كان مسلسل جديد
```

#### ب. إضافة جدول `seasons` لقاعدة البيانات

```sql
CREATE TABLE IF NOT EXISTS seasons (
    series_id TEXT NOT NULL,
    season_number INTEGER NOT NULL,
    topic_id INTEGER,
    name TEXT,
    overview TEXT,
    poster_path TEXT,
    air_date TEXT,
    episode_count INTEGER DEFAULT 0,
    PRIMARY KEY (series_id, season_number),
    FOREIGN KEY (series_id) REFERENCES series(id)
);
```

#### ج. تحديث نمط التعرف

```python
# إضافة استخراج رقم الموسم
def _parse_topic(name: str) -> dict | None:
    if GENERAL_RE.search(name): return None
    
    m = MOVIE_TOPIC_RE.search(name)
    if m: 
        return {
            "type": "movie", 
            "slug": m.group(1), 
            "internal_id": m.group(2), 
            "tmdb_id": int(m.group(3)),
            "topic_title": name
        }
    
    m = SERIES_TOPIC_RE.search(name)
    if m:
        # استخراج رقم الموسم
        season_match = re.search(r'#s(\d+)', name, re.IGNORECASE)
        season_num = int(season_match.group(1)) if season_match else 1
        
        return {
            "type": "series", 
            "slug": m.group(1), 
            "internal_id": m.group(2), 
            "tmdb_id": int(m.group(3)),
            "season_number": season_num,
            "topic_title": name
        }
    
    return None
```

### المرحلة 2: تشغيل Full Scan محسّن

بعد تطبيق الإصلاحات:

```bash
# 1. تطبيق التحديثات على الكود
# 2. تشغيل Full Scan
python3 trigger_fullscan.py

# 3. التحقق من النتائج
python3 check_db_counts.py
```

**النتيجة المتوقعة:**
- ✅ 32 فيلم (كما هو)
- ✅ 9 مسلسلات (بدلاً من 3)
- ✅ جميع المواسم مسجلة
- ✅ جميع الحلقات مرتبطة بشكل صحيح

---

## 📈 الخطة التنفيذية

### الأولوية 1: إصلاح عاجل (اليوم)
1. ✅ فحص المحتوى الفعلي - **تم**
2. ⏳ تحديث `scanner.py` - **التالي**
3. ⏳ إضافة جدول `seasons`
4. ⏳ تشغيل Full Scan
5. ⏳ التحقق من النتائج

### الأولوية 2: تحسينات متقدمة (غداً)
1. تحسين Smart Sync للمواسم الجديدة
2. إضافة واجهة لإدارة المواسم
3. تحسين عرض المسلسلات في Frontend
4. إضافة إشعارات للمواسم الجديدة

### الأولوية 3: مراقبة وصيانة (مستمر)
1. مراقبة المزامنة التلقائية
2. تقارير دورية عن المحتوى
3. تحديثات TMDB للمواسم الجديدة

---

## 🎯 الخلاصة

### ما تم اكتشافه:
1. ✅ **الأفلام:** مزامنة 100% - لا توجد مشاكل
2. ❌ **المسلسلات:** 33% فقط (3 من 9) - مشكلة كبيرة
3. 🔍 **السبب:** نظام المسح لا يتعامل مع المواسم المتعددة بشكل صحيح

### الحل:
- إصلاح منطق التسجيل في `scanner.py`
- إضافة دعم كامل للمواسم
- تشغيل Full Scan محسّن

### النتيجة المتوقعة:
- 🎬 32 فيلم ✅
- 📺 9 مسلسلات ✅
- 📁 25 موسم ✅
- 🎞 جميع الحلقات ✅

---

**الخطوة التالية:** تطبيق الإصلاحات على `scanner.py` وإضافة جدول `seasons`